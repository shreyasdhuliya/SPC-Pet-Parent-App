import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.spc.petparent',
  appName: 'SPC pet parent',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
